name: beaming_yggdrasil
description: Branching data tree for Dart where your data can branch out and thrive
version: 1.0.0

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  rxdart: ^0.28.0
  # path: ^1.8.0

dev_dependencies:
  coverage: ^1.15.0
  lints: ^5.1.1
  test: ^1.24.0
