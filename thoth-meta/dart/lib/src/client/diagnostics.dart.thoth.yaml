locator: lib/src/client/diagnostics.dart
meta:
  architecture_notes: Diagnostics are intentionally generic and side-effect free so observability can evolve outside the core client package.
  class_list:
    - BeamingDiagnosticEvent
  function_list: []
  import_files_list: []
  language: dart
  purpose: Define the structured diagnostics hook contract so callers can observe library activity without committing the package to a logging framework.
  responsibilities: Describe diagnostic event types, event payloads, and optional hook/redaction signatures.
