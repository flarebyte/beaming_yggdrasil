locator: lib/src/client/in_memory.dart
meta:
  architecture_notes: This file intentionally validates API shape and deterministic workflows rather than modeling real transport internals, so do not treat it as the production backend adapter.
  class_list:
    - BeamingInMemoryHarness
    - _InMemoryClient
    - _InMemoryState
    - _InMemoryTestingClient
    - _InMemoryWebSocketSession
  function_list:
    - _identityDiagnostics
    - createBeamingInMemoryHarness
  import_files_list:
    - client.dart
    - dart:async
    - diagnostics.dart
    - model.dart
    - rx_client.dart
    - testing_client.dart
    - websocket.dart
  language: dart
  purpose: Provide an in-memory harness that exercises the public client contracts early without pretending to be the production HTTP or WebSocket implementation.
  responsibilities: Seed snapshots, apply node writes, emit events, expose classic and Rx clients, and offer lightweight diagnostics-aware test scaffolding.
