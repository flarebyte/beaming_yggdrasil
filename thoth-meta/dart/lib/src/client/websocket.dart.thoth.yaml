locator: lib/src/client/websocket.dart
meta:
  architecture_notes: This library stays intentionally thin and delegates the wire details to focused part files so higher-level recovery and policy stay outside the protocol layer.
  class_list: []
  function_list: []
  import_files_list:
    - dart:async
    - error.dart
    - model.dart
  language: dart
  purpose: Define the lightweight WebSocket protocol boundary so real-time session messages can be encoded and decoded without hard-wiring a socket implementation.
  responsibilities: Bind the session interface, client messages, server messages, event envelopes, and shared protocol helpers into one library.
