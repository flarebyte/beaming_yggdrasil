locator: lib/src/client/client.dart
meta:
  architecture_notes: This file is interface-only on purpose so transport, retry, and cache decisions do not leak into the public API too early.
  class_list:
    - BeamingYggdrasilClient
  function_list: []
  import_files_list:
    - model.dart
  language: dart
  purpose: Define the main classic client contract so application code knows the supported high-level operations without depending on a transport implementation.
  responsibilities: Declare snapshot, node, create, and watch APIs for the real client surface.
