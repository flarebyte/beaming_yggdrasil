locator: lib/src/client/model.dart
meta:
  architecture_notes: Values are string-only for now and builders are kept as the explicit construction escape hatch for non-trivial immutable objects.
  class_list:
    - BeamingClientKey
    - BeamingClientKeyBuilder
    - BeamingSetEvent
    - BeamingSnapshotReplacedEvent
    - BeamingValue
    - BeamingValueBuilder
    - BeamingWriteResult
  function_list:
    - BeamingEvent
  import_files_list:
    - error.dart
  language: dart
  purpose: Define the immutable core value, key, result, and event objects that every public client surface shares.
  responsibilities: Represent client keys and values, build validated immutable objects, and carry write and event payloads.
